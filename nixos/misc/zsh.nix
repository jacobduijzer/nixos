{ config, pkgs, ... }:
{
#      programs.zsh {
#              enable = true;
#      };

# ZSH
        #programs.zsh = {
        #       enable = true;

        #       zplug = {
        #               enable = true;
        #               plugins = [
        #                       { name = "zsh-users/zsh-autosuggestions"; }
        #                       #{ name = "romkatv/powerlevel10k"; tags = [ as:theme depth: 1]; }
        #               ];
        #       };
        #};
}
